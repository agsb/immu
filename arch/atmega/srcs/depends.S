;----------------------------------------------------------------------
;   Stack for variables from end sram
;
;----------------------------------------------------------------------
;
; is  ( -- )
;   no checks
;
HEADER "ss-", "ULESS",
    .word UPP, AT, CELL, MINUS, DUP, UPP, TO, ENDS

;----------------------------------------------------------------------
;
; is  ( -- )
;   no checks
;
HEADER "ss+", "UMORE",
    .word UPP, AT, CELL, PLUS, DUP, UPP, TO, ENDS

;----------------------------------------------------------------------
;
; is  ( u -- )
;
HEADER "ss@", "UAT",
    .word UPP, AT, ENDS

;----------------------------------------------------------------------
;
; is  ( u -- )
;
HEADER "ss!", "UTO",
    .word UPP, TO, ENDS

;----------------------------------------------------------------------
;
; is  ( u -- )
;
HEADER ">ss", "USV",
    .word UPP, AT, TO, ENDS

;----------------------------------------------------------------------
;
; is  ( -- u )
;
HEADER "ss>", "ULD",
    .word UPP, AT, AT, ENDS

