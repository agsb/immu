 : LIT ;
 : EXEC ;
 : BRAN ;
 : ZBRAN ;
 : XLOOP ;
 : XPLOO ;
 : XDO ;
 : I ;
 : DIGIT ;
 : PFIND ;
 : ENCL ;
 : EMIT PEMIT ;
 : KEY PKEY ;
 : QTERM PQTER ;
 : CR PCR ;
 : CMOVE ;
 : USTAR ;
 : USLAS ;
 : AND ;
 : OR ;
 : XOR ;
 : SPAT ;
 : SPSTO ;
 : RPSTO ;
 : SEMIS ;
 : LEAVE ;
 : TOR ;
 : FROMR ;
 : R ;
 : ZEQU ;
 : ZLESS ;
 : ZGT ;
 : PLUS ;
 : DPLUS ;
 : MINUS ;
 : DMINU ;
 : OVER ;
 : DROP ;
 : SWAP ;
 : DUP ;
 : PSTOR ;
 : TOGGL ;
 : AT ;
 : CAT ;
 : STORE ;
 : CSTOR ;
 : DOCOL ;
 : DOVAR ;
 : DOCON ;
 : DOUSE ;
 : COLON DOCOL QEXEC SCSP CURR AT CONT STORE CREAT RBRAC PSCOD ;
 : SEMI DOCOL QCSP COMP SEMIS SMUDG LBRAC SEMIS ;
 : CON DOCOL CREAT SMUDG COMMA PSCOD ;
 : VAR DOCOL CON PSCOD ;
 : USER DOCOL ;
 : ZERO DOCON ;
 : ONE DOCON ;
 : TWO DOCON ;
 : THREE DOCON ;
 : BL DOCON ;
 : CL DOCON ;
 : BBUF DOCON ;
 : BSCR DOCON ;
 : PORIG DOCOL LIT PLUS SEMIS ;
 : SZERO DOUSE ;
 : RZERO DOUSE ;
 : TIB DOUSE ;
 : WIDTH DOUSE ;
 : WARN DOUSE ;
 : FENCE DOUSE ;
 : DP DOUSE ;
 : VOCL DOUSE ;
 : FIRST DOUSE ;
 : LIMIT DOUSE ;
 : BLK DOUSE ;
 : IN DOUSE ;
 : OUT DOUSE ;
 : SCR DOUSE ;
 : OFSET DOUSE ;
 : CONT DOUSE ;
 : CURR DOUSE ;
 : STATE DOUSE ;
 : BASE DOUSE ;
 : DPL DOUSE ;
 : FLD DOUSE ;
 : CSP DOUSE ;
 : RNUM DOUSE ;
 : HLD DOUSE ;
 : USE DOUSE ;
 : PREV DOUSE ;
 : ONEP ;
 : TWOP ;
 : ONEM ;
 : TWOM ;
 : HERE DOCOL DP AT SEMIS ;
 : ALLOT DOCOL DP PSTOR SEMIS ;
 : COMMA DOCOL HERE STORE TWO ALLOT SEMIS ;
 : SUB ;
 : PLMNS ;
 : DPLMNS ;
 : EQUAL ;
 : LESS ;
 : GREAT ;
 : ROT ;
 : SPACE DOCOL LIT EMIT SEMIS ;
 : DDUP ;
 : TRAV DOCOL SWAP OVER PLUS LIT OVER CAT LESS ZBRAN DROP SEMIS ;
 : LATES DOCOL CURR AT AT SEMIS ;
 : LFA DOCOL LIT SUB SEMIS ;
 : CFA DOCOL TWO SUB SEMIS ;
 : NFA DOCOL LIT SUB LIT TRAV SEMIS ;
 : PFA DOCOL ONE TRAV LIT PLUS SEMIS ;
 : SCSP DOCOL SPAT CSP STORE SEMIS ;
 : QERR DOCOL SWAP ZBRAN BRAN DROP SEMIS ;
 : QCOMP DOCOL STATE AT ZEQU LIT QERR SEMIS ;
 : QEXEC DOCOL STATE AT LIT QERR SEMIS ;
 : QPAIR DOCOL SUB LIT QERR SEMIS ;
 : QCSP DOCOL SPAT CSP AT SUB LIT QERR SEMIS ;
 : QLOAD DOCOL BLK AT ZEQU LIT QERR SEMIS ;
 : COMP DOCOL QCOMP FROMR DUP TWOP TOR AT COMMA SEMIS ;
 : LBRAC DOCOL ZERO STATE STORE SEMIS ;
 : RBRAC DOCOL LIT STATE STORE SEMIS ;
 : SMUDG DOCOL LATES LIT TOGGL SEMIS ;
 : HEX DOCOL LIT BASE STORE SEMIS ;
 : DEC DOCOL LIT BASE STORE SEMIS ;
 : OCTAL DOCOL LIT BASE STORE SEMIS ;
 : PSCODE DOCOL FROMR LATES PFA CFA STORE SEMIS ;
 : BUILD DOCOL ZERO CON SEMIS ;
 : DOES DOCOL FROMR LATES PFA STORE DODOE ;
 : COUNT DOCOL DUP ONEP SWAP CAT SEMIS ;
 : TYPE DOCOL DDUP ZBRAN PLUS SWAP XDO I CAT EMIT XLOOP BRAN DROP SEMIS ;
 : ECELL DOCOL DUP ONE AND PLUS SEMIS ;
 : DTRAI DOCOL DUP ZERO XDO OVER OVER PLUS ONE SUB CAT BL SUB ZBRAN BRAN ONE SUB XLOOP SEMIS ;
 : PDOTQ DOCOL R COUNT DUP ONEP ECELL FROMR PLUS TOR TYPE SEMIS ;
 : DOTQ DOCOL LIT STATE AT ZBRAN PDOTQ WORD HERE CAT ONEP ECELL ALLOT BRAN WORD HERE COUNT TYPE SEMIS ;
 : QALIG DOCOL HERE ONE AND ALLOT SEMIS ;
 : EXPEC DOCOL OVER PLUS OVER XDO KEY DUP LIT PORIG AT EQUAL ZBRAN LIT OVER I EQUAL DUP FROMR TWO SUB PLUS TOR SUB BRAN DUP LIT EQUAL ZBRAN LEAVE DROP BL ZERO BRAN DUP I CSTOR ZERO I ONEP CSTOR ZERO I TWOP CSTOR EMIT XLOOP DROP SEMIS DROP XLOOP DROP SEMIS ;
 : QUERY DOCOL TIB AT LIT EXPEC ZERO IN STORE SEMIS ;
 : NULL DOCOL BLK AT ZBRAN BLK PSTOR ZERO IN STORE BLK AT BSCR MOD ZEQU ZBRAN QEXEC FROMR DROP BRAN FROMR DROP SEMIS ;
 : FILL DOCOL SWAP TOR OVER CSTOR DUP ONEP FROMR ONE SUB CMOVE SEMIS ;
 : ERASE DOCOL ZERO FILL SEMIS ;
 : BLANK DOCOL BL FILL SEMIS ;
 : HOLD DOCOL LIT HLD PSTOR HLD AT CSTOR SEMIS ;
 : PAD DOCOL HERE LIT PLUS SEMIS ;
 : WORD DOCOL BLK AT ZBRAN AT BLOCK BRAN TIB AT IN AT PLUS SWAP ENCL HERE LIT BLANK IN PSTOR OVER SUB TOR R HERE CSTOR PLUS HERE ONEP FROMR CMOVE SEMIS ;
 : PNUMB DOCOL ONEP DUP TOR CAT BASE AT DIGIT ZBRAN BASE AT USTAR DROP ROT BASE AT USTAR DPLUS DPL AT ONEP ZBRAN ONE DPL PSTOR FROMR BRAN FROMR SEMIS ;
 : NUMB DOCOL ZERO ZERO ROT DUP ONEP CAT LIT EQUAL DUP TOR PLUS LIT DPL STORE PNUMB DUP CAT BL SUB ZBRAN CAT LIT SUB ZERO QERR ZERO BRAN DROP FROMR ZBRAN DMINU SEMIS ;
 : DFIND DOCOL BL WORD HERE COUNT UPPER HERE CONT AT AT PFIND DUP ZEQU ZBRAN HERE LATES PFIND SEMIS ;
 : UPPER DOCOL OVER PLUS SWAP XDO I CAT LIT GREAT I CAT LIT LESS AND ZBRAN LIT TOGGL XLOOP SEMIS ;
 : PABOR DOCOL ABORT SEMIS ;
 : ERROR DOCOL WARN AT ZLESS ZBRAN HERE COUNT TYPE PDOTQ MESS SPSTO IN AT BLK AT QUIT SEMIS ;
 : IDDOT DOCOL PAD LIT LIT 137 FILL DUP PFA LFA OVER SUB PAD SWAP CMOVE PAD COUNT LIT AND TYPE SPACE SEMIS ;
 : CREAT DOCOL DFIND ZBRAN NFA IDDOT LIT MESS SPACE HERE DUP CAT WIDTH AT MIN ONEP ALLOT QALIG DUP LIT TOGGL HERE ONE SUB LIT TOGGL LATES COMMA CURR AT STORE HERE TWOP COMMA SEMIS ;
 : BCOMP DOCOL DFIND ZEQU ZERO QERR DROP CFA COMMA SEMIS ;
 : LITER DOCOL STATE AT ZBRAN LIT SEMIS ;
 : DLITE DOCOL STATE AT ZBRAN LITER LITER SEMIS ;
 : ULESS DOCOL TOR ZERO FROMR ZERO DMINU DPLUS SWAP DROP ZLESS SEMIS ;
 : DLESS DOCOL DMINU DPLUS SWAP DROP ZLESS SEMIS ;
 : QSTAC DOCOL SZERO AT TWO SUB SPAT ULESS ONE QERR SPAT HERE LIT PLUS ULESS TWO QERR SEMIS ;
 : INTER DOCOL DFIND ZBRAN AT LESS ZBRAN CFA COMMA BRAN CFA EXEC QSTAC BRAN HERE NUMB DPL AT ONEP ZBRAN DLITE BRAN DROP LITER QSTAC BRAN  ;
 : IMMED DOCOL LATES LIT TOGGL SEMIS ;
 : VOCAB DOCOL BUILD LIT COMMA CURR AT CFA COMMA HERE VOCL AT COMMA VOCL STORE DOES TWOP CONT STORE SEMIS ;
 : DEFIN DOCOL CONT AT CURR STORE SEMIS ;
 : PAREN DOCOL LIT WORD SEMIS ;
 : QUIT DOCOL ZERO BLK STORE LBRAC RPSTO CR QUERY INTER STATE AT ZEQU ZBRAN BRAN  ;
 : ABORT DOCOL SPSTO DEC SPACE CR PDOTQ FORTH DEFIN QUIT ;
 : NOT DOCOL ZEQU SEMIS ;
 : COLD ;
 : STOD ;
 : ABS DOCOL DUP ZLESS ZBRAN SEMIS ;
 : DABS DOCOL DUP ZLESS ZBRAN SEMIS ;
 : MIN DOCOL OVER OVER GREAT ZBRAN DROP SEMIS ;
 : MAX DOCOL OVER OVER LESS ZBRAN DROP SEMIS ;
 : MSTAR ;
 : MSLAS ;
 : STAR DOCOL MSTAR DROP SEMIS ;
 : SLMOD DOCOL TOR STOD FROMR MSLAS SEMIS ;
 : SLASH DOCOL SLMOD SWAP DROP SEMIS ;
 : MOD DOCOL SLMOD DROP SEMIS ;
 : SSMOD DOCOL TOR MSTAR FROMR MSLAS SEMIS ;
 : SSLA DOCOL SSMOD SWAP DROP SEMIS ;
 : MSMOD DOCOL TOR ZERO R USLAS FROMR SWAP TOR USLAS FROMR SEMIS ;
 : PBUF DOCOL BBUF LIT PLUS PLUS DUP LIMIT AT EQUAL ZBRAN FIRST AT DUP PREV AT SUB SEMIS ;
 : UPDAT DOCOL PREV AT AT LIT OR PREV AT STORE SEMIS ;
 : MTBUF DOCOL FIRST AT LIMIT AT OVER SUB ERASE SEMIS ;
 : FLUSH DOCOL LIMIT AT FIRST AT XDO I AT ZLESS ZBRAN TWOP I AT LIT AND ZERO RW BBUF LIT PLUS XPLOO MTBUF SEMIS ;
 : DRZER DOCOL ZERO OFSET STORE SEMIS ;
 : DRONE DOCOL LIT OFSET STORE SEMIS ;
 : BUFFE DOCOL USE AT DUP TOR PBUF ZBRAN STORE R AT ZLESS ZBRAN R TWOP R AT LIT AND ZERO RW R STORE R PREV STORE FROMR TWOP SEMIS ;
 : BLOCK DOCOL OFSET AT PLUS TOR PREV AT DUP AT LIT AND R SUB ZBRAN ZEQU ZBRAN DROP R BUFFE DUP R ONE RW TWO SUB DUP AT LIT AND R SUB ZEQU ZBRAN DUP PREV STORE FROMR DROP TWOP SEMIS ;
 : PLINE DOCOL TOR CL BBUF SSMOD FROMR BSCR STAR PLUS BLOCK PLUS CL SEMIS ;
 : DLINE DOCOL PLINE DTRAI TYPE SEMIS ;
 : MESS DOCOL WARN AT ZBRAN ZBRAN LIT OFSET AT BSCR SLASH SUB DLINE BRAN PDOTQ DOT SEMIS ;
 : LOAD DOCOL BLK AT TOR IN AT TOR ZERO IN STORE BSCR STAR BLK STORE INTER FROMR IN STORE FROMR BLK STORE SEMIS ;
 : ARROW DOCOL QLOAD ZERO IN STORE BSCR BLK AT OVER MOD SUB BLK PSTOR SEMIS ;
 : TICK DOCOL DFIND ZEQU ZERO QERR DROP LITER SEMIS ;
 : FORGE DOCOL CURR AT CONT AT SUB LIT QERR TICK DUP FENCE AT LESS LIT QERR DUP NFA DP STORE LFA AT CONT AT STORE SEMIS ;
 : BACK DOCOL HERE SUB COMMA SEMIS ;
 : BEGIN DOCOL QCOMP HERE ONE SEMIS ;
 : ENDIF DOCOL QCOMP TWO QPAIR HERE OVER SUB SWAP STORE SEMIS ;
 : THEN DOCOL ENDIF SEMIS ;
 : DO DOCOL COMP XDO HERE LIT SEMIS ;
 : LOOP DOCOL LIT QPAIR COMP XLOOP BACK SEMIS ;
 : PLOOP DOCOL LIT QPAIR COMP XPLOO BACK SEMIS ;
 : UNTIL DOCOL ONE QPAIR COMP ZBRAN SEMIS ;
 : END DOCOL UNTIL SEMIS ;
 : AGAIN DOCOL ONE QPAIR COMP BRAN SEMIS ;
 : REPEAT DOCOL TOR TOR AGAIN FROMR FROMR TWO SUB ENDIF SEMIS ;
 : IF DOCOL COMP ZBRAN COMMA TWO SEMIS ;
 : ELSE DOCOL TWO QPAIR COMP BRAN COMMA SWAP TWO ENDIF TWO SEMIS ;
 : WHILE DOCOL IF TWOP SEMIS ;
 : SPACS DOCOL ZERO MAX DDUP ZBRAN XDO SPACE XLOOP SEMIS ;
 : BDIGS DOCOL PAD HLD STORE SEMIS ;
 : EDIGS DOCOL DROP DROP HLD AT PAD OVER SUB SEMIS ;
 : SIGN DOCOL ROT ZLESS ZBRAN HOLD SEMIS ;
 : DIG DOCOL BASE AT MSMOD ROT LIT OVER LESS ZBRAN PLUS LIT PLUS HOLD SEMIS ;
 : DIGS DOCOL DIG OVER OVER OR ZEQU ZBRAN SEMIS ;
 : DDOTR DOCOL TOR SWAP OVER DABS BDIGS DIGS SIGN EDIGS FROMR OVER SUB SPACS TYPE SEMIS ;
 : DOTR DOCOL TOR STOD FROMR DDOTR SEMIS ;
 : DDOT DOCOL ZERO DDOTR SPACE SEMIS ;
 : DOT DOCOL STOD DDOT SEMIS ;
 : QUEST DOCOL AT DOT SEMIS ;
 : UDOT DOCOL ZERO DDOT SEMIS ;
 : LIST DOCOL DEC CR DUP SCR STORE PDOTQ DOT LIT ZERO XDO CR I THREE DOTR SPACE I SCR AT DLINE XLOOP CR SEMIS ;
 : INDEX DOCOL CR ONEP SWAP XDO CR I THREE DOTR SPACE ZERO I DLINE QTERM ZBRAN XLOOP SEMIS ;
 : TRIAD DOCOL LIT EMIT THREE SLASH THREE STAR THREE OVER PLUS SWAP XDO CR I LIST XLOOP CR LIT MESS CR SEMIS ;
 : VLIST DOCOL LIT OUT STORE CONT AT AT OUT AT LIT GREAT ZBRAN ZERO OUT STORE DUP IDDOT SPACE SPACE PFA LFA AT DUP ZEQU QTERM OR ZBRAN DROP SEMIS ;
 : XVLINK DOCON VLINK ;
 : BYE ;
 : XIO ;
 : BREAD DOCOL ONE XIO SEMIS ;
 : BWRIT DOCOL ZERO XIO SEMIS ;
 : IO DOCOL DUP ONE EQUAL ZBRAN BREAD ZBRAN CR PDOTQ DOT ABORT BRAN ZEQU ZBRAN BWRIT ZBRAN CR PDOTQ DOT ABORT SEMIS ;
 : RW DOCOL TOR TWO STAR OVER OVER ONE SUB R IO SWAP LIT PLUS SWAP FROMR IO SEMIS ;
 : BYE ;
 : XIO ;
 : BREAD DOCOL ONE XIO SEMIS ;
 : BWRIT DOCOL ZERO XIO SEMIS ;
 : IO DOCOL DUP ONE EQUAL ZBRAN BREAD ZBRAN CR PDOTQ DOT ABORT BRAN ZEQU ZBRAN BWRIT ZBRAN CR PDOTQ DOT ABORT SEMIS ;
 : RW DOCOL TOR TWO STAR OVER OVER ONE SUB R IO SWAP LIT PLUS SWAP FROMR IO SEMIS ;
 : BYE ;
 : RLCYL DOCOL ZERO LIT USLAS SWAP DROP SEMIS ;
 : RLHEAD DOCOL LIT AND LIT SLASH SEMIS ;
 : RLSEC DOCOL LIT AND SEMIS ;
 : RLCHS DOCOL LIT SLMOD LIT USTAR DROP SWAP LIT STAR OR SEMIS ;
 : RLSTAT ;
 : RLHDR ;
 : RLDIR DOCOL DUP ZLESS ZBRAN ONE BRAN LIT SEMIS ;
 : RLDIFF DOCOL DUP ROT RLCYL TOR RLCYL FROMR SUB RLDIR SWAP LIT USTAR DROP OR SWAP RLHEAD LIT STAR OR SEMIS ;
 : RLSEEK ;
 : RLSET DOCOL RLCHS DUP RLHDR SWAP RLDIFF RLSEEK LIT AND SEMIS ;
 : RLRD ;
 : RLWR ;
 : RLERR DOCOL PDOTQ BASE AT TOR LIT AT OCTAL UDOT PDOTQ RLSTAT OCTAL UDOT FROMR BASE STORE SEMIS ;
 : ARW DOCOL SWAP RLSET DROP SWAP ZBRAN LIT AND ZBRAN QUIT BRAN RLWR LIT AND ZBRAN QUIT SEMIS ;
 : RW DOCOL SWAP LIT PLUS SWAP ARW SEMIS ;
 : NRTS ;
 : NWTS ;
 : RTS DOCOL ONE NRTS ZBRAN QUIT SEMIS ;
 : WTS DOCOL ONE NWTS ZBRAN QUIT SEMIS ;
 : SKEW1 DOCOL DUP LIT SLASH SWAP OVER LIT STAR OVER DUP PLUS PLUS SWAP LIT MOD LIT SLASH PLUS LIT MOD ONEP SWAP ONEP SWAP SEMIS ;
 : SSKIP DOVAR ;
 : SUSED DOVAR ;
 : SKEW DOCOL DUP ONEP SUSED AT GREAT ZBRAN AT SUB SSKIP AT PLUS SKEW1 SWAP LIT PLUS SWAP BRAN SSKIP AT PLUS SKEW1 SEMIS ;
 : NSET DOCOL ROT OVER LIT STAR PLUS ROT ROT OVER PLUS ONE SUB SWAP ONE SUB SWAP XDO LIT SUB DUP I SKEW ROT LIT XPLOO DROP SEMIS ;
 : RW DOCOL TOR ONE SUB LIT STAR FROMR ZBRAN NSET LIT NRTS ZBRAN PDOTQ QUIT BRAN LIT NSET LIT NWTS ZBRAN PDOTQ QUIT SEMIS ;
 : TRAPS DOCOL CR PDOTQ DOT SWAP UDOT UDOT QUIT ;
 : SEMIC DOCOL QCSP COMP PSCOD LBRAC SMUDG SEMIS ;
 : FORTH DODOE DOVOC ;
 : TASK DOCOL SEMIS ;
 : ORIGIN ;
